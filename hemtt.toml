name = "A4ES"
prefix = "a4es"
author = "SzwedzikPL oraz twórcy addonów zewnętrznych"
mainprefix = "z"
files = [
    "*.dll",
    "*.so",
    "mod.cpp",
    "README.md",
    "AUTHORS.txt",
    "LICENSE",
    "logo_a4es.paa",
    "logosmall_a4es.paa",
    "picture_a4es.paa",
    "version.a4e"
]

modname = "{{name}}"
key_name = "{{prefix}}_{{version}}"
authority = "{{prefix}}_{{version}}-{{git \"id 8\"}}"

releasebuild = [
    "@zip a4es_{{semver.major}}.{{semver.minor}}.{{semver.patch}}"
]

[header_exts]
version= "{{git \"id 8\"}}"
