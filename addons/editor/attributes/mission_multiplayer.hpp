class Lobby {
    class Attributes {
        class DisabledAI {
            control = QGVAR(hiddenCheckbox);
        };
        class JoinUnassigned {
            control = QGVAR(hiddenCheckbox);
        };
    };
};
